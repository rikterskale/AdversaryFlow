.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.test-tmp/
.adversaryflow/
.ruff_cache/
.coverage
htmlcov/
build/
dist/
*.egg-info/
.env
*.log
.DS_Store

# Local agent/assistant settings. These are per-developer and have previously
# reached a source distribution; the sdist allowlist now blocks that, but they
# must not be committed either.
.claude/
.agents/

# Local ATT&CK snapshots can be large and should be pinned separately.
data/enterprise-attack.json

# Keep the canonical demo artifacts; ignore additional generated reports.
reports/*
!reports/apt29_scenario.md
!reports/apt29_scenario.trace.json
